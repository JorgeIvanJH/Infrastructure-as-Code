# Terraform downloads and local state
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.tfstate.lock.info

# Local variable values
terraform.tfvars
variables.pkrvars.hcl

# Local SSH keys used by lesson 4
tf-packer
tf-packer.pub

# Tool crash logs
crash.log
crash.*.log
