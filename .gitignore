# Terraform downloads and local state
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.tfstate.lock.info

# Local variable values
terraform.tfvars
variables.pkrvars.hcl

# Local Guacamole login and connection settings
user-mapping.xml

# Local SSH keys used by the GCP image lessons
tf-packer
tf-packer.pub

# Tool crash logs
crash.log
crash.*.log
